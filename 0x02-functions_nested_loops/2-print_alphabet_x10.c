#include <stdio.h>
/**
 *   main - Entry point
 *
 *  Return: Always 0 (Success)
 */
void print_alphabet_x10(void);
{
	int n;
	char ch;

	for (n == 0; n < 10; n++)
	{
		for (ch = 'a'; ch <= 'z'; ch++)
		{
			putchar(ch);
		}
		putchar ('\n');
	}
}
