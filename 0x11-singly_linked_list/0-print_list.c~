#include "lists.h"

size_t print_list(const list_t *h)
{
	size_t i;

	for (i = 0; h != NULL; i++)
	{
		if (h->str != NULL)
		{
			printf("[%u]%s", h->len, h->str);
		}
		else
		{
			printf("[0](nil)");
		}
		h = h->next;
	}
	return(i);
}
