#include "holberton.h"
/**
 * _strpbrk - searches a string for a set of bytes
 * @s: string im looking for
 * @accept: string im looking though
 * Return: accept
 */
char *_strpbrk(char *s, char *accept)
{
	return (accept);
}
